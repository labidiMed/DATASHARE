COMPOSE_PROJECT_NAME=datashare

# PostgreSQL (service db, chargé via env_file)
POSTGRES_DB=datashare
POSTGRES_USER=datashare
POSTGRES_PASSWORD=change_me

# Front-end Vite (service frontend, chargé via env_file)
VITE_API_URL=http://localhost:8000/api/v1
